---
title: CodeNode 阶段 0 插件制作记录
type: implementation-log
stage: 0
status: partial
tags:
  - CodeNode
  - Codex-plugin
  - Java
  - implementation
---

# 阶段 0：Codex 插件制作记录

## 目标

建立可安装的 Codex 插件骨架，并验证 Java 第一阶段所需的工具链、Skill 协议和最小示例工作流。

## 已制作内容

- `.codex-plugin/plugin.json`：插件清单、版本、入口说明和默认提示词。
- `skills/codenode-java/SKILL.md`：环境检查、示例生成、节点输入协议和安全边界。
- `scripts/check-java-env.ps1`：检查 Java、`javac`、Maven Wrapper、Git 和工作目录。
- `scripts/create-java-demo.ps1`：生成 `AddIntegers` Java 类、JUnit 5 测试和 `pom.xml`。
- `README.md`：插件用途、目录结构和本地检查命令。
- 个人 Codex 插件市场中的 `codenode` 条目及缓存版本标记。
- `skills/codenode-workflow-dsl/SKILL.md`：全语言嵌套工作流 DSL、AST、环境变量和安全规则。
- `schemas/`：节点、边、工作流请求和工作流结果 JSON Schema。
- `scripts/classify-maven-diagnostics.mjs`：Maven 依赖/插件错误的项目级诊断分类。
- `scripts/test-java-demo.ps1`：固定 JDK 21、Maven Wrapper 的可重复测试入口。

## 验收记录

| 验收项 | 结果 | 说明 |
| --- | --- | --- |
| 插件清单存在且结构合法 | 通过 | `name=codenode`，版本带 Codex cachebuster |
| Skill frontmatter 存在 | 通过 | `codenode-java` 可被插件扫描 |
| 环境检查脚本可运行 | 通过 | 能输出结构化 JSON |
| 示例项目生成脚本可运行 | 通过 | 已生成 `stage0-demo` |
| Java 21 编译测试 | 通过 | JDK 21.0.9；Maven Wrapper 3.9.10 下 6 个测试通过 |
| Maven 测试 | 通过 | `mvnw.cmd -B test`：6 tests, 0 failures, 0 errors |
| JDK Compiler API 结构化诊断 | 通过 | 输出文件、行、列、诊断码；包含成功/失败测试 |
| Maven 错误分类 | 通过 | 依赖解析和插件解析错误归类为项目级诊断 |
| 工作流/节点/边/结果 JSON Schema | 通过 | `schemas/` 四份 Schema 可解析并完成引用检查 |
| 变量计算、条件分支、JSON 转换金丝雀 | 通过 | `CanaryWorkflowTest` 三项均通过 |
| Agent 请求规范化 20 次本地验收 | 通过 | `agent-schema-canary.mjs`：20/20，100% |
| 真实 Agent 结构化输出 20 次稳定性 | 待处理 | 本地脚本不能替代真实 Codex Agent 重复调用 |
| 千级节点数据模型基准 | 通过 | `benchmark-workflow.mjs` 支持 1,000 节点测量 |
| React Flow 千级节点验证 | 待处理 | 当前为零依赖原生画布原型，尚未进行 React Flow 性能对照 |
| Codex CLI 自动安装/刷新 | 受阻 | CLI 进程返回“拒绝访问” |
| 可视化节点画布 | 后续阶段 | 阶段 0 不伪装为已完成 |

## 本地检查

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\codenode\scripts\check-java-env.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\codenode\scripts\create-java-demo.ps1 -OutputDirectory ..\stage0-demo-2
```

## 关联文档

- [[欢迎]]
- [[CodeNode项目执行方案]]

## 下一步验收清单

- [x] 安装 Java 21 LTS 并确认 `java -version`。
- [x] 确认 `javac -version` 和 Maven Wrapper 可用。
- [ ] 在 Codex 新任务中加载 `codenode-java`。
- [x] 运行示例项目并记录 `mvnw.cmd -B test` 输出。
- [x] 验证 JDK Compiler API 结构化诊断和 Maven 项目级错误分类。
- [x] 验证工作流/节点/边 JSON Schema 与嵌套 DSL 解析器。
- [x] 完成 20 个本地结构化请求规范化验收和 1,000 节点数据模型基准。
- [ ] 完成 Agent 20 次结构化输出稳定性测试。
- [ ] 完成 React Flow/千级节点性能技术验证。
- [ ] 开始实现 Codex 内节点画布原型。
